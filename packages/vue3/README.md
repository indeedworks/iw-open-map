# @indeedworks/open-map-vue3

OpenMap 的 Vue 3 Composition API 适配组件。地图业务能力由 `@indeedworks/open-map` 提供。

```bash
pnpm add @indeedworks/open-map @indeedworks/open-map-vue3
```

```ts
import { OpenMapView } from '@indeedworks/open-map-vue3';
import '@indeedworks/open-map/style.css';
```
